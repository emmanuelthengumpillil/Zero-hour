You are a senior Full stack developer and you should build the software as you are directed.

You are building a website
Name :- 'Zero Hour'

Here is my planned architecture:

- AI Tool: Antigravity
- Stack: Next.js  + JS + Python 
- Email Provider: Resend
- Database: Supabase (PostgreSQL) for storing email logs
- Flow: after signing in the users gets to see a demo, in the screen there are 2 boxes, 1- contacts, 2-customers, As a demo the user will be able to drag and drop the name card(his name, email) to customers box and an email is send to him showing welcome to Zero Hour etc, the email can be customised,
- Hosting: Vercel

Requirements:
Use OAuth 2.0 framework and google sign in to sign in easily, 

There should be a page for building the content to be sent to the emails. 
A button to send and ask confirmation before sending.


Note:
keep in mind, you should ask permisson to change any of this


Architecture:
Fine-grained Role-Based Access Control — RBAC
1- me the owner of this software: Full access, even to all the admin
2- The admin, Business owner: has access to everything in their level adn below, can access the owner of the software, see analysis based on his usings 


Few requirements for production ready systems.
Data Protection: Encrypt sensitive data both at rest (AES-256) and in transit (TLS 1.3). Store secrets securely using dedicated key management services (AWS Secrets Manager, HashiCorp Vault), never hardcoded or in Git

Input Validation & Sanitization: Sanitize all user inputs to prevent common injection attacks (SQLi, XSS, CSRF, Command Injection).

Graceful Degradation & Fallbacks: Design the system to handle component failures cleanly (e.g., if a recommendation engine fails, return popular items instead of crashing the page).

Resilience Patterns: Implement Circuit Breakers, Retry Logic with Exponential Backoff, and Rate Limiting to protect internal and downstream services from traffic spikes or cascading failures.